@startuml
!function $outer($x)
  !local $inner_val = $x * 2
  !return $inner_val + 1
!endfunction

!function $chain($a, $b)
  !local $sum = $a + $b
  !local $doubled = $sum * 2
  !return $doubled
!endfunction

note as N
  outer(5) = $outer(5)
  chain(3, 4) = $chain(3, 4)
end note
@enduml
