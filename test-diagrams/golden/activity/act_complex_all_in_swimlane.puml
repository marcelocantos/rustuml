@startuml

|User|
start
:Login;
|System|
if (authenticated?) then (yes)
  fork
    |Audit|
    :Log access;
    fork again
    |System|
    while (session active?) is (yes)
      :Handle request;
    endwhile
  end fork
else (no)
  :Reject;
endif
|User|
stop
@enduml
