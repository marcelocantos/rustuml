@startuml
start
if (cond1?) then (yes)
  if (cond2?) then (yes)
    if (cond3?) then (yes)
      :detach trigger;
      detach
    else (no)
      :Alt;
    endif
  else (no)
    :Alt;
  endif
else (no)
  :Alt;
endif
stop
@enduml
