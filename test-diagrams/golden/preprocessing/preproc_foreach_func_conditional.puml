@startuml
!function $classify($svc)
  !if $svc == "DB"
    !return "storage"
  !else
    !return "logic"
  !endif
!endfunction

!foreach $svc in ["API", "DB", "Cache"]
  class $svc {
    type: $classify($svc)
  }
!endforeach
@enduml
