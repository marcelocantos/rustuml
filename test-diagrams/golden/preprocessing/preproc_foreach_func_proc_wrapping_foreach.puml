@startuml
!procedure $drawChain($prefix, $steps)
  !foreach $s in $steps
    :$prefix $s;
  !endforeach
!endprocedure

start
$drawChain("Phase A:", ["init", "validate", "run"])
$drawChain("Phase B:", ["cleanup", "report"])
stop
@enduml
