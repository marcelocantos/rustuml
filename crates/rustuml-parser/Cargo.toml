[package]
name = "rustuml-parser"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libc = "0.2"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yml = "0.0.12"
